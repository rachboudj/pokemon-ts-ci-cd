name: GitHub Actions Demo

on: [push, pull_request]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."

      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."

      # - name: Set up SSH agent
      #   uses: webfactory/ssh-agent@v0.9.0
      #   with:
      #     ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}  # Ajoutez votre clé privée SSH dans les secrets de GitHub

      # 4. Ajouter le serveur distant à known_hosts pour éviter les problèmes de sécurité
      # uses: webfactory/ssh-agent@v0.9.0
      # with:
      #   ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
      # name: Adding Known Host
      #   run: ssh-keyscan -H 152.228.134.113 >> ~/.ssh/known_hosts
      # name: Ls on server
      #   run: ssh debian@152.228.134.113 'ls -la /var/www'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      # - name: Install dependencies
      #   run: npm ci

      # - name: Check Prettier formatting
      #   run: npm run format:check

      # - name: Run ESLint
      #   run: npm run lint

      # - name: Check TypeScript types
      #   run: npm run type-check

      # - name: Build the project
      #   run: npm run build

      # - name: List files in the repository
      #   run: |
      #     ls ${{ github.workspace }}

      - run: echo "🍏 This job's status is ${{ job.status }}."
